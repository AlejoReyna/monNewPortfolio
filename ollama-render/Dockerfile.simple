FROM ollama/ollama:latest

# Instalar curl
USER root
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# Exponer puerto
EXPOSE 11434

# Script simple inline
USER ollama
CMD ["sh", "-c", "ollama serve & sleep 10 && ollama pull llama3.1:8b && wait"]
